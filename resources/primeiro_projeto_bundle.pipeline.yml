# The main pipeline for primeiro_projeto_bundle
resources:
  pipelines:
    primeiro_projeto_bundle_pipeline:
      name: primeiro_projeto_bundle_pipeline
      catalog: workspace
      schema: primeiro_projeto_bundle_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
