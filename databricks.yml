# This is a Databricks asset bundle definition for primeiro_projeto_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: primeiro_projeto_bundle
  uuid: d1fbfc07-5b52-42e9-86d2-86061e860317

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-741e4be9-a51c.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-741e4be9-a51c.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/tiagocesar68@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/tiagocesar68@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: tiagocesar68@gmail.com
        level: CAN_MANAGE
